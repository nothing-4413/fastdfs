# client

The `client` module implements the command line tool.

## Main Responsibility

The client provides a simple CLI interface for testing and operating the system.

Supported commands:

```text
ping
upload
download
delete
stat
sync