# common

The `common` module contains utilities shared by tracker, storage and client.

## Main Responsibility

Currently this module mainly provides config parsing.

The config parser supports a simple format:

```ini
key = value