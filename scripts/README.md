# scripts

This directory stores helper scripts for local development and testing.

## Suggested Scripts

| Script | Purpose |
|---|---|
| `clean_data.ps1` | Clean runtime data |
| `start_tracker.ps1` | Start tracker |
| `start_storage1.ps1` | Start storage1 |
| `start_storage2.ps1` | Start storage2 |
| `demo.ps1` | Run a simple demo |

## Notes

For Windows PowerShell, use:

```powershell
New-Item -ItemType Directory -Force data/storage1/files